# Database

The prototype currently uses in-memory UI state only. This folder is reserved for trip, profile, vehicle, and emergency-service persistence when the backend becomes connected to a database.
